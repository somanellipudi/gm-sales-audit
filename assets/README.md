# PDF Brand Assets

Place the GrowingMonk logo here as:

- `growingmonk_logo.png`

Optional mark-only asset:

- `growingmonk_mark.png`

MonkAudit PDF exports automatically use `growingmonk_logo.png`. If no logo file is present, PDFs use a built-in GrowingMonk wordmark fallback.
